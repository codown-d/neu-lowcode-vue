{
  "private": true,
  "name": "neu-lowcode-vue",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm --filter=material-render build",
    "dev": "pnpm --filter=admin dev"
  },
  "workspaces": [
    "admin",
    "material-render"
  ],
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}